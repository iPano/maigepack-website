{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cmd=\"$1\"; if echo \"$cmd\" | grep -qE \"git push.*(origin )?(main|develop)\\b\" && ! echo \"$cmd\" | grep -q \"\\-u\"; then echo \"⚠️  Do not push directly to main or develop. Work on a feature branch and open a PR to develop for review.\"; exit 2; fi' -- \"$CLAUDE_TOOL_INPUT_COMMAND\""
          }
        ]
      }
    ]
  }
}
