# Use Java 18 for Spring Boot
providers = ["java"]

[phases.setup]
nixPkgs = ["jdk18"]

[phases.build]
cmds = ["./gradlew build -x test"]

[phases.start]
cmd = "java -jar -Dspring.profiles.active=prod build/libs/*.jar"

[variables]
NIXPACKS_JAVA_VERSION = "18"